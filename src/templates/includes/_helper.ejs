<%
helper = {
    today: () => {
        return (new Date()).toLocaleDateString('ja-JP');
    },
    asset: (
            /** @type {string} */ path
    ) => {
        return meta.path.relative.asset + '/' + path;
    }
}
%>
